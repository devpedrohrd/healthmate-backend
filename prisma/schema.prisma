generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ProfissionalSaude {
  id            Int         @id @default(autoincrement())
  nome          String      @db.VarChar(100)
  email         String      @unique @db.VarChar(100)
  senha         String      @db.VarChar(255)
  especialidade String?     @db.VarChar(100)
  foto_perfil   String?     @db.VarChar(255)
  googleId      String?     @db.VarChar(255)
  createdAt     String?     @db.VarChar(255)
  Pacientes     Paciente[]
  Relatorio     Relatorio[]

  @@index([googleId, id], name: "googleId")
}

model Paciente {
  id             Int                 @id @default(autoincrement())
  nome           String              @db.VarChar(100)
  email          String              @unique @db.VarChar(100)
  telefone       String?             @db.VarChar(15)
  endereco       Json                @db.Json
  senha          String?             @db.VarChar(255)
  foto_perfil    String?             @db.VarChar(255)
  createdAt      String?             @db.VarChar(255)
  googleId       String?             @db.VarChar(255)
  Profissional   ProfissionalSaude[]
  Medicamento    Medicamento[]
  AvaliacaoSaude AvaliacaoSaude[]
  Relatorio      Relatorio[]

  @@index([googleId, id], name: "googleIdPaciente")
}

model Medicamento {
  id         Int        @id @default(autoincrement())
  pacienteId Int        @map("paciente_id")
  nome       String     @db.VarChar(100)
  descricao  String?
  dosagem    String?    @db.VarChar(50)
  horario    String?    @db.VarChar(50)
  quantidade Int        @default(0)
  createdAt  String?    @db.VarChar(255)
  paciente   Paciente   @relation(fields: [pacienteId], references: [id])
  Lembrete   Lembrete[]

  @@index([pacienteId, id], name: "medicamentoIdPaciente")
}

model Lembrete {
  id            Int         @id @default(autoincrement())
  medicamentoId Int         @map("medicamento_id")
  horaLembrete  String?     @map("hora_lembrete")
  titulo        String?     @db.VarChar(100)
  mensagem      String?     @db.VarChar(255)
  createdAt     String?     @db.VarChar(255)
  status        String      @default("pendente") @db.VarChar(20)
  medicamento   Medicamento @relation(fields: [medicamentoId], references: [id])

  @@index([medicamentoId, id], name: "lembreteIdMedicamento")
}

model AvaliacaoSaude {
  id                Int      @id @default(autoincrement())
  pacienteId        Int      @map("paciente_id")
  sintomas          String?
  efeitosColaterais String?  @map("efeitos_colaterais")
  observacoes       String?
  createdAt         String?  @db.VarChar(255)
  paciente          Paciente @relation(fields: [pacienteId], references: [id])

  @@index([pacienteId, id], name: "avalicaoIdPaciente")
}

model Relatorio {
  id                  Int               @id @default(autoincrement())
  pacienteId          Int               @map("paciente_id")
  profissionalSaudeId Int               @map("profissional_saude_id")
  conteudoRelatorio   String?           @map("conteudo_relatorio")
  createdAt           String?           @db.VarChar(255)
  paciente            Paciente          @relation(fields: [pacienteId], references: [id])
  profissionalSaude   ProfissionalSaude @relation(fields: [profissionalSaudeId], references: [id])

  @@index([pacienteId, id], name: "pacienteIdRelatorio")
}
